## Reversi

This project implements the board game Reversi using Python's `PyQt5` GUI library and the `pyo3` Rust interface for
Python.

Only the bot side of the project is made in Rust.

-----

### Installing

Install dependencies: `pip install -r requirements.txt`

Compiling the Rust code: `maturin develop -r`, inside the `bot/src` directory.

